# 2026.03.02 (월)

💿 다른 파일에서 모듈을 불러오다가 일어난 일

모듈을 단순히 대화형 인터프리터 창에서만 말고, 다른 파일에서 불러와서 사용할 수도 있다. 어떻게 하는지 배우는 와중 일어난 일이다

뭐 솔직히 어려운건 없었다

mod1이라는 모듈을 먼저 만들었다
#mod1
def add(a, b):
  return a + b

그리고 mod2라는 모듈을 만들어서 mod1을 가져와서 사용한다는 내용을 작성했다
#mod2
import mod1
result = mod1.add(3, 4)
print(result)

작성은 IDLE에 에디터창에서 이루어졌고, 두 파일 mod1, mod2는 내 맥북 바탕화면에 저장을 해둔 상태였다
그리고 mod2.py파일을 열어 작동시켰다

그렇다면 7이라는 결과값이 출력되는 아주 간단한 예시였다, 그리고 나도 그걸 예상했고, 근데 왠걸 오류가 뜨는거다
ModuleNotFoundError: No module named 'mod1'

아니 분명 저장도 다했고, py파일에, 파일들이 같은 위치에 있고, 내가 import할 때도 mod1.py가 아니라 mod1이라는 모듈의 이름만 적었는데...???

눈이 빠지게 오타를 찾아도, 오타도 없었고, 반복해서 저 오류만 떳다

GPT에게 도움을 요청하니 지금 Shell창의 위치가 어디인지 확인해보라고, 아니면 어쩌고 저쩌고 하면서 길고 긴 설명과 무수한 코드를 뿜어냈다

그 중에 일단 시도해본게 IDLE Shell창에 입력한 현재 위치가 어디인지 묻는 코드였다
import os
print(os.getcwd())
당연히 내 사용자명과 어디에 위치해있는지 자연스럽게 나왔다. Desktop 바탕화면에 위치해있었다.

내가 쓰려는 파일이 있는 곳에 위치해 있어야 import를 쓸 수 있다고 해서 두 파일을 같은 곳에 두었고, 나도 Desktop에 위치해 있는데 도대체 뭔 일인지 알수 가없었다

파일 자체가 문제인가 해서, 파일을 지우고 써보기도 했고, IDLE을 종료했다 켜보기도 했다

아니 근데 무슨 실행이 안되는거임;;;; 진짜 개 열받음....

그래서 맥의 terminal에서 실행해보니 결과값 7이 똭~~~~ 하.......

뭐 GPT 설명으로는 맥에서 IDLE이 종종 오류가 난다고, 그래서 VS CODE 그걸 쓴다고 하는데, 그게 맞는 설명인지도 모르겠다

일단, 터미널에서 파일들이 정상적으로 작동했다는 것을 보면 확실한건 파일의 문제가 아닌데, 그렇다면 IDLE에서 내가 뭘 잘못 손댄건지, 원래 그런건지 모르겠다
내가 진~~~~~짜 오늘 짜증났던건, 파일이 오류인줄 알고 헛수고를 너무 많이했는데, 정작 터미널에서는 작동되고, 그래서 IDLE과 맥의 호환 문제인가 혹은 다른 원인이 있나 하고 찾아본건데
결국은 해결책과 원인을 알아내지 못했다는 것이다

하.... 말이 이렇게 간단하지 진짜 시간 오래걸렸다... 근데 알아낸게 없으니 원.... 뭐 항상 유의미한 결과물을 얻을 수는 없고, 실무를 해도 오늘과 같이 헛수고 하는 날이 분명 있을 것이긴 하다
그래도 개열받는 건 어쩔 수 없는 것 같다
